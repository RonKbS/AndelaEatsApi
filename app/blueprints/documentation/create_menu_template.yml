Create a Menu Template
---
tags:
  - Menu Template
summary: Creates Menu Template.
consumes:
  - application/json
parameters:
  - name: X-Location
    type: integer
    required: true
    description: The center's location Id
    in: header
    example: '1'
  - name: Authorization
    type: string
    required: true
    description: User's token prefix with the word "Bearer"
    in: header
  - name: menu_template
    in: body
    required: true
    description: Creation of Menu Template
    schema:
      type: object
      required:
        - name
        - description
        - mealPeriod
      properties:
        name:
          type: string
          example: name
        mealPeriod:
          type: string
          enum: [breakfast, lunch]
          example: lunch
        description:
          type: string
          example: somethind
definitions:
  MenuTemplatePayload:
    properties:
      name:
        type: string
      locationId:
        type: integer
        format: int32
      mealPeriod:
        type: string
  Timestamps:
    properties:
      created_at:
        type: string
      updated_at:
        type: string
  Payload:
    properties:
      id:
        type: integer
        format: int32
      isDeleted:
        type: boolean
      name:
        type: string
      locationId:
        type: integer
        format: int32
      mealPeriod:
        type: string
      timestamps:
        $ref: '#/definitions/Timestamps'
  Model1:
    properties:
      msg:
        type: string
      payload:
        $ref: '#/definitions/Payload'

responses:
  201:
    description: Ok
    schema:
      $ref: '#/definitions/MenuTemplatePayload'
    examples:
      response: |
        {
          "msg": "string",
          "payload": {
            "id": 1,
            "isDeleted": true,
            "name": "string",
            "locationId": 1,
            "mealPeriod": "string",
            "timestamps": {
              "created_at": "string",
              "updated_at": "string"
            }
          }
        }